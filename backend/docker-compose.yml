services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: foodmatch
      POSTGRES_PASSWORD: foodmatch
      POSTGRES_DB: foodmatch
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
